import { CreateAccountForUserModel } from './create-account-for-user-model';

describe('CreateAccountForUserModel', () => {
  it('should create an instance', () => {
    expect(new CreateAccountForUserModel()).toBeTruthy();
  });
});
