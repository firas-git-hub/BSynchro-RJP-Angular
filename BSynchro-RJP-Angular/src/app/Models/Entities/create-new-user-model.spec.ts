import { CreateNewUserModel } from './create-new-user-model';

describe('CreateNewUserModel', () => {
  it('should create an instance', () => {
    expect(new CreateNewUserModel()).toBeTruthy();
  });
});
